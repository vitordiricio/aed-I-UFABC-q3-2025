// Faça um programa que recebe um inteiro como entrada e imprime o seu valor na tela.
#include <stdio.h>

int main(void) {
    int num;
    printf("Entrada: ");
    scanf("%d", &num);
    printf("Saída: %d", num);
    return 0;
}